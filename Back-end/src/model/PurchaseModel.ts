export interface PurchaseDTO{
    quantity:number,
    clientId:string,
    productId:string
}

export interface Purchase{
    id:string,
    quantity:number,
    clientId:string,
    productId:string
}